name: lovespace
description: A romantic app for couples.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  # أيقونات
  cupertino_icons: ^1.0.2
  # فايربيز (قاعدة البيانات)
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  # رفع الوسائط (Uploadcare)
  uploadcare_flutter: ^4.0.0
  # تنسيق التاريخ للرسائل
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

